# Model artifact

The trained `.keras` model is intentionally excluded from this public repository. It contains approximately 37 MB of weights and can be regenerated by running the notebook with the private dataset.

Verified interface:

- Input: `(None, 224, 224, 3)`
- Output: `(None, 3)`
- Classes: `A`, `B`, `C`
